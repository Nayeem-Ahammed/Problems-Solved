#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ios::sync_with_stdio(false); cin.tie(nullptr);
    ll t = 1; cin >> t;

    while (t--) {
        ll n; cin >> n;
        vector<int> h(n), c(n);
        for(auto &i : h) cin >> i;
        for(auto &i : c) cin >> i;
        
        auto get = [&](int x) {
            int64_t cost = 0;
                
        };

    }
    return 0;
}